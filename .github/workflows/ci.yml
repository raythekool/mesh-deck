name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: Test Python 3.11
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install locked dependencies
        run: uv sync --locked

      - name: Lint
        # Pinned: an unpinned linter turns a ruff release into a red build on
        # an unrelated PR. Bump deliberately.
        run: uvx ruff@0.16.8 check src tests tools

      - name: Run tests
        run: uv run python -m unittest discover -s tests -v
